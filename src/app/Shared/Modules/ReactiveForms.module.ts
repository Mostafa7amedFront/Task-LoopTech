import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { ReactiveFormsModule, FormsModule } from '@angular/forms';

export const ReactiveModeuls = [
  CommonModule,
  RouterModule,
  ReactiveFormsModule,
  FormsModule
];